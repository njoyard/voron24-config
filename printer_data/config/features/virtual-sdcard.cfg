[virtual_sdcard]
path: /home/nicolas/printer_data/gcodes
on_error_gcode:
  {% if "xyz" in printer.toolhead.homed_axes %}
    _PARK_TOOLHEAD POS=park
  {% endif %}
  
  CANCEL_PRINT

  _SET_LIGHT_STATUS STATUS=error
  RESPOND MSG="Error in GCode."
