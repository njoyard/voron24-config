[gcode_macro VERBOSE]
gcode:
  {% set vars = printer["gcode_macro _USER_VARIABLES"] %}

  {% if vars.verbose %}
    RESPOND {% for p in params %}{'%s="%s" ' % (p, params[p])}{% endfor %}
  {% endif %}

  {% if 'MSG' in params %}
    SET_DISPLAY_TEXT MSG="{params.MSG}"
  {% endif %}
