[gcode_macro _PREHEAT]
gcode:
  {% set bed = params.BED %}
  {% set hotend = params.HOTEND %}
  {% set chamber = params.CHAMBER | default(0) %}



[gcode_macro _HEAT]
gcode:
  {% set bed = params.BED %}
  {% set hotend = params.HOTEND %}
  {% set chamber = params.CHAMBER | default(0) %}

  